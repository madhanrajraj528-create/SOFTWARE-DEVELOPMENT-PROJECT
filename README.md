# SOFTWARE-DEVELOPMENT-PROJECT
End-to-end Python + SQL pipeline analyzing retail sales data — generates transactions, loads into SQLite, runs analytical queries, and visualizes revenue trends by region, category, and customer. A data analytics pipeline that, Built with Python and SQL, data generation, database loading, aggregation queries, and visualization
